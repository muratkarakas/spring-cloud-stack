spring:
  application:
    name: sc-zipkin
  cloud:
    config:
      uri: http://localhost:8888
      
      
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://configserver:8888           